/*
for (var i = 0; i < ds_list_size(global.systems); i++)
{
	part_system_destroy(global.systems[| i]);
}
ds_list_destroy(global.systems);

for (var i = 0; i < ds_list_size(global.emitters); i++)
{
	part_emitter_destroy(global.systems[| i], global.emitters[| i]);
}
ds_list_destroy(global.emitters);*/